# Aluno: 122475 – Pedro Vicêncio
# Quality Gate: Significant Duplication (Identity Disharmony)

name: Qodana – Significant Duplication

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  qodana:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Qodana for JVM
        uses: JetBrains/qodana-action@v2023.3
        with:
          args: >
            --fail-threshold,0
